import {GET_DATA, LIKE, DISLIKE} from '../actions/userAction';

const initialState={
    data: null
};

export default function(state=initialState, action){
    switch(action){
        //todo
    }

    return state;
}